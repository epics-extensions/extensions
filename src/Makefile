ADD_ON = ..

# the order of the following directories  is important

# tools dependent on config         ---  all tools with PROD set
# tools dependent on sdds           ---  burt, knobconfig, rampload, orbitscreen
# tools dependent on dalib (libdev) ---  burt, condition, DP, math
# tools dependent on ca             ---  gus, math, pvwave, snaps, wingz, xmca
#

SUB_DIRS =	alh ar ca cau dalib gvxi km mdct medm probe sdds xmseq \
			burt condition dp knobconfig math orbitscreen pvwave rampload snaps \
			stripTool wingz xmca

include $(ADD_ON)/src/config/HRULES
